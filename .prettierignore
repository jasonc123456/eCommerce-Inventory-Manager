node_modules/
dist/
.next/
coverage/
pnpm-lock.yaml
packages/db/drizzle/
CHANGELOG.md
# Regenerated by Next.js on every build and carries its own "do not edit"
# notice. Formatting it would be undone by the next build, so the repository
# would flap between formatted and unformatted with nobody touching it.
next-env.d.ts
